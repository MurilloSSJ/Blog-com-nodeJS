<div style="min-height:100vh;">
<div class="p-5 mb-4 bg-light rounded-3 container mt-4">
      <div class="container-fluid py-5 text-center" style="width: 60%;">
        <h1 class="display-5 fw-bold text-center">Example blog</h1>
        <p class="fs-4 mt-4">Using a series of utilities, you can create this jumbotron, just like the one in previous versions of Bootstrap. Check out the examples below for how you can remix and restyle it to your liking.</p>
        <div class="text-center mt-5">
        <button class="btn btn-primary btn-lg text-center" type="button">Crie uma conta!</button>
        </div>
      </div>
</div>
<div class="container text-center">
    <hr>
    <h2>Ultimos Posts:</h2>
    <div>
        {{#each}}
        {{else}}
            <small>Nenhuma postagem cadastrada ainda!</small>
        {{/each}}
    </div>
</div>

</div>